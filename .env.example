# Example Environment Variables
# Copy this file to .env.local and fill in your actual values

# Supabase Configuration (Required)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Analytics & Error Tracking (Optional)
# These services are optional - the app will work without them
# Only add these if you want to enable analytics and error tracking

# Sentry Error Tracking (Optional)
# Get your DSN from: https://sentry.io/settings/projects/
VITE_SENTRY_DSN=
VITE_SENTRY_DEBUG=false

# PostHog Analytics (Optional)  
# Get your key from: https://app.posthog.com/project/settings
VITE_POSTHOG_KEY=
VITE_POSTHOG_DEBUG=false

# Stripe Payment Processing (Optional)
# Required only if you're using payment features
VITE_STRIPE_PUBLISHABLE_KEY=

# API Configuration (Optional)
# External API endpoints - configure if you're using external services
VITE_API_BASE_URL=https://api.pulsespark.ai

# Development Settings
# These are typically only needed in development
VITE_ENABLE_MSW=false
VITE_LOG_LEVEL=info